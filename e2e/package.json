{
  "name": "happy-e2e-tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:live-sessions": "playwright test tests/live-sessions.spec.ts",
    "test:live-sessions:headed": "playwright test tests/live-sessions.spec.ts --headed",
    "test:smoke": "playwright test tests/smoke.spec.ts",
    "test:auth": "playwright test tests/auth.spec.ts",
    "show-report": "playwright show-report",
    "build-cli": "docker compose --profile manual build cli",
    "clean": "docker compose down && docker volume prune -f"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
